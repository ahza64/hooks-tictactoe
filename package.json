{
  "name": "tictactoe",
  "version": "0.1.0",
  "description": "",
  "scripts": {
    "fix-parcel": "npx npm-force-resolutions && npm i && npm i",
    "start": "npm run clear && parcel index.html",
    "build": "npm run clear && parcel build index.html",
    "clear": "rimraf dist .parcel-cache"
  },
  "resolutions": {
    "@babel/preset-env": "7.13.8"
  },
  "license": "MIT",
  "dependencies": {
    "react": "^16.13.1",
    "react-dom": "^16.13.1"
  },
  "devDependencies": {
    "@babel/core": "^7.15.5",
    "babel-preset-nano-react-app": "^0.1.0",
    "parcel": "1.12.4",
    "sass": "^1.42.1"
  },
  "babel": {
    "presets": [
      "nano-react-app"
    ],
    "plugins": [
      [
        "@babel/plugin-proposal-class-properties",
        {
          "loose": true
        },
        "yt1"
      ],
      [
        "@babel/plugin-proposal-private-methods",
        {
          "loose": true
        },
        "yt2"
      ]
    ]
  }
}
